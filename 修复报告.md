# 安全漏洞修复报告

## 项目概述

本报告记录了"用户信息管理平台"从 **v1.0（漏洞演示版）** 到 **v2.0（安全加固版）** 的完整安全修复过程。

| 版本 | 路径 | 说明 |
|:----:|------|------|
| v1.0 | [./1.0](./1.0) | 原始版本，刻意保留安全漏洞用于教学演示 |
| v2.0 | [./2.0](./2.0) | 安全加固版本，修复了全部 12 个漏洞 |

---

## 修复清单

### 🔴 严重漏洞（P0）

| 编号 | 漏洞描述 | 涉及文件 | 严重程度 |
|:----:|---------|---------|:--------:|
| 01 | 密码明文存储 | `1.0/app.py` USERS 字典 | 🔴 严重 |
| 02 | 密码明文比对 | `1.0/app.py` login 路由 | 🔴 严重 |
| 03 | 密码明文展示在页面 | `1.0/templates/index.html` | 🔴 严重 |
| 04 | Secret Key 硬编码且弱 | `1.0/app.py` secret_key | 🔴 严重 |

### 🟠 高危漏洞（P1）

| 编号 | 漏洞描述 | 涉及文件 | 严重程度 |
|:----:|---------|---------|:--------:|
| 05 | Debug 模式开启 | `1.0/app.py` debug=True | 🟠 高危 |
| 06 | 绑定 0.0.0.0 全网暴露 | `1.0/app.py` host 参数 | 🟠 高危 |
| 07 | 无暴力破解防护 | `1.0/app.py` login 路由 | 🟠 高危 |
| 08 | 无 CSRF 防护 | `1.0/templates/login.html` | 🟠 高危 |

### 🟡 中危漏洞（P2）

| 编号 | 漏洞描述 | 涉及文件 | 严重程度 |
|:----:|---------|---------|:--------:|
| 09 | HTML 注释泄露默认账号 | `1.0/templates/login.html` | 🟡 中危 |
| 10 | Session 无过期机制 | `1.0/app.py` 配置 | 🟡 中危 |
| 11 | 无日志审计 | `1.0/app.py` | 🟡 中危 |
| 12 | 无输入校验 | `1.0/app.py` login 路由 | 🟡 中危 |

---

## 修复详情

### 漏洞 01：密码明文存储

**问题代码（v1.0）：**
```python
USERS = {
    "admin": {
        "password": "admin123",   # 明文存储
    }
}
```

**修复方案（v2.0）：**
```python
from werkzeug.security import generate_password_hash

USERS = {
    "admin": {
        "password": generate_password_hash("admin123"),  # bcrypt 哈希
    }
}
```

**原理：** `generate_password_hash()` 使用 bcrypt 加盐哈希，即使数据库泄露也无法逆向还原。

---

### 漏洞 02：密码明文比对

**问题代码（v1.0）：**
```python
if USERS[username]["password"] == password:  # 直接 == 比对
```

**修复方案（v2.0）：**
```python
if check_password_hash(user["password"], password):  # 安全哈希比对
```

**原理：** 常量时间比较，防止时序攻击。

---

### 漏洞 03：密码明文展示在页面

**问题代码（v1.0 index.html）：**
```html
<li><span class="label">密码：</span>{{ user.password }}</li>
```

**修复方案（v2.0）：** 新增 `get_safe_user_info()` 函数过滤 password 字段，模板中删除密码行。

---

### 漏洞 04：Secret Key 硬编码

**问题代码（v1.0）：**
```python
app.secret_key = "dev-key-2025"  # 公开已知的固定字符串
```

**修复方案（v2.0）：**
```python
import secrets
app.config["SECRET_KEY"] = secrets.token_hex(32)  # 256 位随机密钥
```

**攻击演示：**
```bash
flask-unsign --sign --cookie "{'username':'admin'}" --secret "dev-key-2025"
```

---

### 漏洞 05：Debug 模式开启

**v1.0：** `app.run(debug=True, host="0.0.0.0", port=5000)`
**v2.0：** `app.run(debug=False, host="127.0.0.1", port=5000)`

Werkzeug 调试器 `/__debugger__` 端点可能导致远程代码执行。

---

### 漏洞 06：绑定 0.0.0.0

**v1.0：** `host="0.0.0.0"` — 所有网络接口可达
**v2.0：** `host="127.0.0.1"` — 仅本机可访问

---

### 漏洞 07：无暴力破解防护

**v1.0：** 登录接口无任何限制
**v2.0：** 集成 `flask-limiter`，每分钟最多 10 次尝试，超限返回 HTTP 429

```python
@limiter.limit("10 per minute")
def login():
    ...
```

---

### 漏洞 08：无 CSRF 防护

**v1.0：** POST 请求无 CSRF token
**v2.0：** 启用 `CSRFProtect(app)`，每个表单嵌入 csrf_token

无 token 的 POST 直接返回 HTTP 400。

---

### 漏洞 09：HTML 注释泄露账号

**v1.0：**
```html
<!-- 调试信息 - 默认管理员账号 用户名: admin 密码: admin123 -->
```
**v2.0：** 已删除，替换为安全通用提示。

---

### 漏洞 10：Session 无过期机制

**v1.0：** 未配置超时，默认 31 天
**v2.0：** `SESSION_PERMANENT=False`，`PERMANENT_SESSION_LIFETIME=2 小时`

---

### 漏洞 11：无日志审计

**v1.0：** 无任何日志
**v2.0：** 集成 logging，记录登录成功/失败/登出操作及 IP 地址

```
2026-07-19 [INFO] Failed login for 'admin' from 192.168.1.100
2026-07-19 [INFO] Successful login for 'admin' from 127.0.0.1
```

---

### 漏洞 12：无输入校验

**v1.0：** `request.form.get()` 直接使用
**v2.0：** 空值检测 + 用户名 64 字符、密码 128 字符长度限制

---

## 修复统计

| 统计项 | 数值 |
|--------|:----:|
| 发现漏洞总数 | 12 |
| 已修复漏洞数 | 12 |
| 严重漏洞（P0） | 4 |
| 高危漏洞（P1） | 4 |
| 中危漏洞（P2） | 4 |
| 新增依赖包 | 3（werkzeug, flask-wtf, flask-limiter） |

---

## 最终项目结构

```
/opt/Class01/
├── 1.0/                       # v1.0 - 漏洞演示版
│   ├── app.py                 #   明文密码 + 无防护
│   ├── templates/
│   ├── static/
│   ├── setup.sh
│   └── README.md
│
├── 2.0/                       # v2.0 - 安全加固版
│   ├── app.py                 #   哈希密码 + 完整防护
│   ├── templates/
│   ├── static/
│   ├── setup.sh
│   ├── requirements.txt
│   └── README.md
│
└── 修复报告.md                 # 本文件
```

---

*报告生成日期：2026-07-19*
